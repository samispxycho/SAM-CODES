package LINKED_LIST;

public class MiddleOfLL {

    //Slow tortoise Fast tortoise
    /*
    public ListNode middleNode(ListNode head) {
        ListNode fast=head;
        ListNode slow=head;
        while(fast!=null && fast.next!=null){
            slow = slow.next;
            fast = fast.next.next;
        }
        return slow;
    }
    */

    public static void main(String[] args) {
        
    }
}
